# ============================================================
# AI 内容工场 · 后端完整依赖清单
# 安装：pip install -r requirements.txt
# 说明：所有依赖均为生产必需，无冗余；版本下限已按 Python 3.10+ 兼容锁定
# ============================================================

# ---------- 核心框架 ----------
fastapi>=0.115.0            # 高性能异步 Web 框架（自动接口文档 / 依赖注入）
uvicorn[standard]>=0.32.0   # ASGI 服务器（含 uvloop/httptools 加速）
pydantic>=2.10.0            # 数据校验与配置模型（Schema 强约束基石）
pydantic-settings>=2.6.0    # 环境变量/.env 配置加载

# ---------- 数据持久层（SQLite / MySQL 双兼容） ----------
sqlalchemy>=2.0.36          # ORM（SQLite WAL 并发优化，MySQL 仅切 URL）

# ---------- AI 能力层 ----------
httpx>=0.28.0               # Ollama / OpenAI 兼容接口统一 HTTP 调用
python-multipart>=0.0.12    # FastAPI 文件上传（批量导入）

# ---------- 安全与鉴权 ----------
PyJWT>=2.10.0               # 无状态 JWT 鉴权（口令散列用标准库 PBKDF2）

# ---------- 文档处理 ----------
python-docx>=1.1.2          # Word 标准化导出（交付级排版）
openpyxl>=3.1.5             # Excel 批量导入与结果打包下载

# ---------- 生产环境数据库（本地开发无需安装，按需取消注释） ----------
# PyMySQL>=1.1.1            # MySQL 驱动
# cryptography>=44.0.0      # MySQL 认证加密

# ---------- 可选：Celery 分布式任务（默认线程池无需安装） ----------
# celery>=5.4.0
# redis>=5.2.0              # Celery broker（也可用 RabbitMQ）
