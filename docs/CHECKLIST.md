# 开发自测 Checklist（逐项打勾，发布前全绿）

> 用法：每轮发布前按此清单过一遍；标题项违规 = 阻断发布。

## 1. 环境与启动（✅ 必须全绿）

- [x] `python backend/start.py --check` 体检通过（Python/Node/端口/Ollama）
- [x] Windows 双击 `start.bat` 一键启动成功（无乱码、无报错弹窗）
- [x] macOS/Linux `./start.sh` 可启动
- [x] Docker：`docker compose up -d --build` 后端健康检查通过后前端才启动
- [x] 后端默认端口被占用时自动顺延（8000-8009），日志显示实际端口
- [x] 首次运行自动生成 `backend/.env`，改配置无需重启（运行时配置接口）
- [x] `npm run build` 生产构建通过（本机内存紧张时用 `npm run build:lowmem`；Docker 容器内无此问题）

## 2. 认证与权限

- [x] 初始管理员 `admin / admin123` 自动创建（幂等：已有同名账号则提升+重置）
- [x] 登录页仅登录（无注册入口）；密码错误返回 401 + 中文提示
- [x] 修改密码：原密码校验、两次一致、生效后新密码可登录
- [x] 未登录访问业务页 → 重定向登录页；登录后回跳原页面
- [x] 管理页仅管理员可见（导航隐藏入口 + 路由守卫 + 后端双重校验）
- [x] 非管理员调 /admin 接口 → 403

## 3. 模块1 短视频脚本

- [x] 主题/平台/时长/风格参数完整校验（空主题、非法平台、超长主题有前端 toast）
- [x] 字数范围（min/max）约束生效，min>max 前端拦截
- [x] 时长强约束：分段时长之和 == 视频时长（自动对齐）
- [x] 结果四 Tab：分镜脚本 / 爆款标题 / 话题标签 / TTS 配音文稿
- [x] 操作按钮组：重新生成 / 局部微调 / 导出 Word / 保存为模板 / 保存至历史
- [x] 每段台词/每组标题/标签/配音文本 均可单独复制（toast 反馈）
- [x] 生成失败 → 错误卡片（含排查指引）+ 重试按钮
- [x] 骨架屏加载 + 本地模型冷启动提示文案

## 4. 模块2 标题标签

- [x] 10 组差异化标题 + 3+3+6 层级标签（不足自动补足）
- [x] 二次润色（基于已有标题重写）
- [x] 重新生成 / 导出 Word / 保存为模板
- [x] 演示模式可零模型验证

## 5. 模块3 文案改写

- [x] 7 种动作 × 6 风格 + 自定义风格模板带入
- [x] 原文 10-5000 字校验 + 5000 字超限红色计数提示
- [x] 重新处理 / 导出 Word / 保存为模板 / 一键复制
- [x] 失败错误卡片

## 6. 模块4 批量生成

- [x] TXT/CSV/XLSX 上传解析；格式错误 → 上传区错误卡片 + toast
- [x] 导入模板下载（batch_template.txt）
- [x] 单任务上限 50 条：输入计数 + 到达上限禁用按钮
- [x] 并发上限 2：超限新任务排队（展示排队提示）
- [x] 任务卡：ID/命名/创建时间/总数/成功/失败/耗时/状态（排队/生成/完成/部分失败/全部失败/取消）
- [x] 操作：查看结果弹窗（条目级成功/失败+原因）/ 重试失败 / 下载 Excel / 下载 Word(zip) / 终止（确认弹窗）/ 删除（确认弹窗）
- [x] 进度条实时更新（3s 轮询），单条失败不中断整批

## 7. 模块5/6 TTS 与导出

- [x] 规则引擎：剔除书面语/填充词、长句断句、出镜/旁白识别、字数与朗读时长估算
- [x] Word 导出成功 toast 反馈；文档含标准样式（标题/小标题/正文/分镜表格/页码）
- [x] 批量/勾选导出 zip 前有「文件生成中，请稍候」提示
- [x] 演示数据不落库（导出需真实生成）

## 8. 模块7 历史记录

- [x] 检索（关键词/类型/平台）+ 分页
- [x] 卡片：勾选出批量导出(zip) / 批量移入回收站（确认弹窗）
- [x] 单条：查看 / 复用（回填脚本页）/ Word / 软删除（确认+可恢复）/ 永久删除（确认）
- [x] 恢复接口（回收站 API 层保留）

## 9. 模块8 模板库

- [x] 三类模板 Tab + 卡片列表
- [x] 使用模板（风格→改写页自定义风格；脚本/Prompt→脚本页自定义风格）
- [x] 编辑 / 复制内容 / 删除（确认弹窗）
- [x] 生成页「保存为模板」入口（命名+场景+描述）

## 10. 全局反馈与状态

- [x] Toast：成功/错误/警告全覆盖（生成、复制、导出、删除、配置保存…）
- [x] ConfirmDialog：高危操作二次确认（软删/永久删/终止任务/删模板/删用户）
- [x] 降级提示条：云端不可用→本地；全不可用→演示模式（点按打开状态弹窗）
- [x] 导航栏：模型状态指示点（点击→状态弹窗+排查指引）、演示数据开关、改密、管理入口
- [x] 模型配置弹窗：Provider 链开关、Ollama 地址/模型、云端地址/Key/模型、温度/Token/超时/批量参数，保存即生效

## 11. 异常与边界

- [x] Ollama 未启动：红点提示 + 排查指引 + 自动降级 Mock
- [x] 云端 Key 未配置：提示条说明降级原因
- [x] 文案超长：前端 maxlength + 红色计数 + 提示
- [x] 批量文件损坏/格式错误：上传区错误卡片
- [x] 输入为空提交：toast 拦截，不发出请求

## 12. 回归与部署产物

- [x] `tests/smoke_test.py` 全链路通过（26 项：认证/八模块/demo/配置/管理/批量重试/打包导出）
- [x] 前端生产构建 0 错误；`dist/` 产物生成
- [x] README：一键启动 / Docker / Ollama 指引 / API-Key 说明 / 面试演示指南
- [x] documentation 完整（本清单 + README）
